<div class="container">
  <div id="flash-section">
    <% flash.each do |name, msg| %>
      <% unless msg == true %>
        <%= content_tag :div, msg, :class => "alert alert-#{name}" %>
      <% end %>
    <% end %>
  </div>
</div>
