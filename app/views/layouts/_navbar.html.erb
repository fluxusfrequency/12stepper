<nav class="navbar navbar-fixed-top navbar-inverse" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to t("title"), root_path, id: "logo-link", class: "navbar-brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse navbar-ex1-collapse">
      <ul class="nav navbar-nav">

        <li class="dropdown">
          <%= link_to t("navbar.resources"), "#", {:id => "resources-link", :class => "dropdown-toggle", 'data-toggle' => "dropdown"}%>
          <b class="caret"></b>
          <ul class="dropdown-menu">
            <li><%= link_to t("navbar.twelvesteps"), page_path("twelve-steps-#{I18n.locale.to_s}") %>
            </li>
            <li><%= link_to t("navbar.twelvetraditions"), page_path("twelve-traditions-#{I18n.locale.to_s}") %>
            </li>
            <li><%= link_to t("navbar.serenityprayer"), page_path("serenity-prayer-#{I18n.locale.to_s}") %>
            </li>
            <li><%= link_to t("navbar.bigbook"), big_book_chapters_path %>
            </li>
          </ul>
        </li>
        <li><%= link_to t("navbar.meetings"), meetings_path, id:"meetings-link" %>
        </li>
        <li><%# link_to t("navbar.journal", "#", id:"journal-link") %>
        </li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>
        <li><%= link_to t('navbar.congrats') + 
          " on #{current_user.days_of_sobriety} " + 
          t('navbar.sober') + "!", "#" %></li>
          <li class="dropdown">
            <%= link_to t('navbar.welcome') + ", " + 
              current_user.username + "!", "#", {:id => "resources-link", 
              :class => "dropdown-toggle", 'data-toggle' => "dropdown"}%>

            <b class="caret"></b>
            <ul class="dropdown-menu">
              <li><%= link_to t("navbar.logout"), sessions_path, method: :delete %>
              </li>
            </ul>
          </li>
        <% else %>
          <%= link_to t("navbar.signup"), new_user_path %>
          <%= link_to t("navbar.login"), new_session_path %>
        <% end %>
      </ul>
    </div>
    <!-- /.navbar-collapse -->
  </div>
  <!-- /.container -->
</nav>

