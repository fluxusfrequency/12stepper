<%= form_for @entry do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.cktext_area :body, {:class => 'ckeditor', :id => 'worksheet-step'} %>

  <%= f.submit t('entries.create') %>
  
<% end %>
<div class='worksheet col-lg-5'>
hello
</div>
